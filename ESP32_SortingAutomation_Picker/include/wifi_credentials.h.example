#pragma once
// Template kredensial WiFi/OTA -- copy file ini jadi "wifi_credentials.h" (tanpa .example)
// lalu isi nilai asli. File "wifi_credentials.h" sengaja di-gitignore supaya password
// tidak pernah masuk ke git history repo publik/tim.
#define WIFI_SSID     "GANTI_SSID"
#define WIFI_PASSWORD "GANTI_PASSWORD"
#define OTA_PASSWORD  "GANTI_OTA_PASSWORD"
#define OTA_HOSTNAME  "esp32-picker"
